stages:
  - build
  - deploy

variables:
  NODE_VERSION: "22"

before_script:
  - apt-get update -y
  - apt-get install -y openssh-client

build-job:
  stage: build
  image: node:$NODE_VERSION
  script:
    - npm install
    - npm run generate
    - ls -la dist/
  artifacts:
    paths:
      - dist/
    expire_in: 1 week

deploy-to-vps:
  stage: deploy
  only:
    - main
  script:
    - mkdir -p ~/.ssh
    - echo "$VPS_SSH_KEY" | tr -d '\r' > ~/.ssh/id_rsa
    - chmod 600 ~/.ssh/id_rsa
    - ssh-keyscan -H $VPS_HOST >> ~/.ssh/known_hosts
    - ls -la  # Vérification du répertoire de travail actuel
    - ls -la dist/  # Vérification du contenu du répertoire dist pour confirmer la présence des fichiers
    - if [ -z "$(ls -A dist)" ]; then echo "Le répertoire dist est vide. Annulation du déploiement."; exit 1; fi
    - ssh -o StrictHostKeyChecking=no $VPS_USER@$VPS_HOST "sudo rm -rf /var/www/portfolio/*"
    - scp -o StrictHostKeyChecking=no -r dist/* $VPS_USER@$VPS_HOST:/var/www/portfolio/
  dependencies:
    - build-job
  environment:
    name: production
    url: http://$VPS_HOST
